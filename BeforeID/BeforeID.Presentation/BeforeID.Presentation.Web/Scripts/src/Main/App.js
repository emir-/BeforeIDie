(function (bid, $) {

    var app = (function () {

        var init = function(parameters) {

        };

        return {
            Init: init
        };
    })();

    bid.App = app;
    
})(BID = window.BID || {}, jquery);